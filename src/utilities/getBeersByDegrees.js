const getBeersByDegrees = (valor) => {
  let allDegreesBeerValue;
  let decimal;

  switch (valor) {
    case "NA":
      allDegreesBeerValue = 0;
      return allDegreesBeerValue;
    case "0":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "1":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "2":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "3":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "4":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "5":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "6":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "7":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "8":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "9":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "10":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "11":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "12":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "13":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "14":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "15":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "16":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "17":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "18":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "19":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    case "20":
      allDegreesBeerValue = [valor];

      for (decimal = 1; decimal < 10; decimal += 1) {
        allDegreesBeerValue.push(`${valor}.${decimal}`);
      }
      return allDegreesBeerValue;
    default:
      allDegreesBeerValue = [];
      return allDegreesBeerValue;
  }
};

module.exports = getBeersByDegrees;
